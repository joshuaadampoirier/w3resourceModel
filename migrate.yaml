# database metadata
engine: 'SQLite3'
dbname: 'w3resourceModel'

# execute table definitions 
tables:
  - always: False 
  - recursive: False 
  - order:
    - 'Agents.sql'
    - 'Customer.sql'
    - 'Orders.sql'

# execute migrations 
migrations:
 - always: False 
 - recursive: False 