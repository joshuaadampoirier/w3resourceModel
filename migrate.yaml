# type of database project
engine: 'SQLite3'

# execute table definitions 
tables:
  - always: False 
  - recursive: False 
  - order:
    - 'Agents.sql'
    - 'Customer.sql'
    - 'Orders.sql'

# execute migrations 
migrations:
 - always: False 
 - recursive: False 